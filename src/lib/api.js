// const BASE_URL = "https://rickandmortyapi.com/api/character";

// export function fetchFilter(status) {
//   let url;

//   if (status === "alive") {
//     url = `${url}?status=alive`;
//   } else if (status === "dead") {
//     url = `${url}?status=dead`;
//   } else if (status === "unknown") {
//     url = url;
//   }
//   return fetch(url).then((res) => res.json());
// }
